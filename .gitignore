*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Built artefacts. APKs are 10-16 MB each and belong on a release page, not in
# the history: once committed they stay in every future clone forever.
*.apk
*.aab
*.idsig

# Kotlin compiler crash dumps.
/.kotlin

# Release signing. Losing the keystore means never being able to update the
# published app again - the repo doubles as its backup.
*.keystore
!cashbookbd-release.keystore
*.jks
keystore.properties
